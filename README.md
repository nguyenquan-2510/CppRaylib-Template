# CppRaylib-Template
A template to code C++ with raylib in vscode, using g++

It will auto compile all the cpp files in the src folder,
auto detect extra header files in include
and support raylib library
